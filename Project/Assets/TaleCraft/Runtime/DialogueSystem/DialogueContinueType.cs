namespace TaleCraft.Dialogue
{
    /// <summary>
    /// Defines how a dialogue node should behave.
    /// </summary>
    public enum DialogueContinueType
    {
        Button, // Press a button
        Wait    // Wait for certain time
    }
}